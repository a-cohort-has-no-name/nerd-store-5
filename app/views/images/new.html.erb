<h3>New image for <%= @product.name %></h3>

<%= form_tag "/products/#{@product.id}/images", method: :post do %>
  <div>
    Url: <input type="text" name="url">
  </div>
  <input type="submit" value="Create Image">
<% end %>